services:
  frontdoor:
    build: .
    env_file: [runtime.env]
    environment: { ROLE: frontdoor, MODE: patched }
    ports: ["127.0.0.1:19091:8080"]
    networks: [edge, internal]
    read_only: true
    tmpfs: [/tmp]
  records:
    build: .
    env_file: [runtime.env]
    environment: { ROLE: records, MODE: patched }
    networks: [internal]
    read_only: true
    tmpfs: [/tmp]
  admin:
    build: .
    env_file: [runtime.env]
    environment: { ROLE: admin, MODE: patched }
    networks: [internal]
    read_only: true
    tmpfs: [/tmp]
networks:
  edge: {}
  internal: { internal: true }
