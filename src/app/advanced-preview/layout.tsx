import type { ReactNode } from "react";
import "../subdomains/advanced-theme.css";

export const metadata = { title: "Local Advanced Stage Design Review | TRAN" };

export default function LocalAdvancedPreviewLayout({ children }: { children: ReactNode }) {
  return children;
}
