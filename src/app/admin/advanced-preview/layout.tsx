import type { ReactNode } from "react";
import "../../subdomains/advanced-theme.css";

export const metadata = { title: "Advanced Stage Design Review | TRAN" };

export default function AdvancedPreviewLayout({ children }: { children: ReactNode }) {
  return children;
}
