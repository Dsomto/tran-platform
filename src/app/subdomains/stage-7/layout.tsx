import type { ReactNode } from "react";
import "../advanced-theme.css";

export const metadata = { title: "Advanced Project 3 - Architecture | TRAN" };

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
