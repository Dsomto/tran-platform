import type { ReactNode } from "react";
import "../advanced-theme.css";

export const metadata = { title: "Advanced Project 5 - The Final Case | TRAN" };

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
