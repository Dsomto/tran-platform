import type { ReactNode } from "react";
import "../subdomains/advanced-theme.css";

export const metadata = { title: "Advanced Stage — Page Review | TRAN" };

export default function StagePreviewLayout({ children }: { children: ReactNode }) {
  return children;
}
